# 单仓库 Turborepo 基础教程

如果简单安装的话使用

```shell
pnpm dlx create-turbo@latest
```

这个起始仓库包含两个可部署的应用程序 `/apps` ，三个共享仓库 `/packages`

## 安装 `turbo`

`turbo` 可以全局安装，[也可以安装在您的仓库中](https://turbo.net.cn/docs/guides/running-tasks#automatic-package-scoping)。我们强烈建议两种方式都安装，以便您可以利用快速便捷的工作流，并为仓库中的所有开发人员提供一个稳定的 `turbo` 版本。

### [全局安装](https://turbo.net.cn/docs/getting-started/installation#global-installation)

全局安装 `turbo` 为您的本地工作流带来了灵活性和速度。

```shell
pnpm add turbo --global
```

一旦全局安装，您就可以通过 `turbo` 从终端运行您的脚本，快速运行一次性命令以在您的仓库中使用。例如：

- `turbo build`：根据仓库的依赖图运行 `build` 脚本
- `turbo build --filter=docs --dry`：快速打印 `docs` 包的 `build` 任务概述（不运行它）
- `turbo generate`：运行[生成器](https://turbo.net.cn/docs/guides/generating-code)以向您的仓库添加新代码
- `cd apps/docs && turbo build`：在 `docs` 包及其依赖项中运行 `build` 脚本。更多信息请访问[自动包范围划分部分](https://turbo.net.cn/docs/crafting-your-repository/running-tasks#automatic-package-scoping)。\
  **当然也可以在根目录的packages.json 内的** `scripts`**配置使用node的包管理器来运行这些指令**

# 从示例开始

使用 `create-turbo` 通过你喜欢的工具引导一个示例。

```shell
# Use an example listed below
pnpm dlx create-turbo@latest --example [example-name]  # 示例名称

# Use a GitHub repository from the community
pnpm dlx create-turbo@latest --example [github-url] #示例在github的地址
```

# 添加到现有仓库

Turborepo 可以以增量方式引入到**任何存储库，无论是单包还是多包**，以加速存储库的开发人员和 CI 工作流程。

安装 `turbo` 并在 `turbo.json` 中配置任务后，您会注意到[缓存](https://turbo.net.cn/docs/crafting-your-repository/caching)如何帮助您更快地运行任务。

## [准备单包工作区](https://turbo.net.cn/docs/getting-started/add-to-existing-repository#preparing-a-single-package-workspace)

[单包工作区](https://vercel.com/docs/vercel-platform/glossary#single-package-workspace)是例如运行 `npx create-next-app` 或 `npm create vite` 后得到的结果。您无需额外操作即可让 Turborepo 处理您的存储库，因此您可以直接跳到下面的第一步。

要了解有关单包工作区中 Turborepo 的更多信息，请访问[专用指南](https://turbo.net.cn/docs/guides/single-package-workspaces)。

## [准备多包工作区（monorepo）](https://turbo.net.cn/docs/getting-started/add-to-existing-repository#preparing-a-multi-package-workspace-monorepo)

`turbo` 构建在 Workspaces 之上，这是 JavaScript 生态系统中主要包管理器的一个特性。这使得它易于在现有代码库中采用。

![waring]

> [!IMPORTANT]
>
> 如果您发现 `turbo` 存在无法在工作区中发现包或未遵循您的依赖图等问题，请访问我们的[组织存储库](https://turbo.net.cn/docs/crafting-your-repository/structuring-a-repository)页面以获取提示。

请注意，您不必立即开始使用 `turbo` 运行*所有*包的*所有*任务。您可以从几个包中的单个任务开始，随着您对 Turborepo 越来越熟悉，逐步添加更多任务和包。

## [将 Turborepo 添加到您的存储库](https://turbo.net.cn/docs/getting-started/add-to-existing-repository#adding-turborepo-to-your-repository)

[安装 ](https://turbo.net.cn/docs/getting-started/add-to-existing-repository#install-turbo)`turbo`

如果已经有仓库的话，可以直接安装`tubro`到根目录，如果没有的话，先新建项目文件夹，在开始安装之前，必须在已有的仓库和新建的仓库根目录下新建 `pnpm-workspace.yaml`

```shell
mkdir test && cd test
touch pnpm-workspace.yaml
# 全局安装 如果已经全局安装可忽略
pnpm add turbo --global
# 安装在仓库
pnpm add turbo --save-dev --workspace-root
```

[添加 ](https://turbo.net.cn/docs/getting-started/add-to-existing-repository#add-a-turbojson-file)`turbo.json` 文件

在存储库的根目录中，创建一个`torbo.json`文件。

本指南将使用 `build` 和 `check-types` 任务，但您可以将其替换为其他您感兴趣的任务，例如 `lint` 或 `test`。

```json
{
  "$schema": "https://turbo.net.cn/schema.json",
  "tasks": { //定义 Turborepo 可以执行的任务。每个键对应一个任务名称
    "build": {
      "dependsOn": ["^build"], //使用 ^ 微语法，表示先运行依赖包中的 build 任务，再运行当前包的 build 任务
      "outputs": [".next/**", "!.next/cache/**"]//指定缓存的输出文件和目录，这里会缓存 dist 目录下的所有文件
    },
    "check-types": {
      "dependsOn": ["^check-types"] //同bulid 检查项目ts有没有报错
    },
    "dev": {
      "persistent": true,//标记为持久化任务，会一直运行而不会退出
      "cache": false//不缓存此任务的结果（因为开发服务器是长期运行的）
    }
  }
}
```

在您的 Next.js 应用程序中，确保您有一个 `check-types` 脚本供 `turbo` 运行。

```shell
{
  "scripts": {
+   "check-types": "tsc --noEmit"
  }
}
```

在多包工作区中，您可能还希望在您的一个或多个库包中添加 `check-types` 脚本，以查看多个脚本如何在不同包中通过一个 `turbo` 命令运行。

### [编辑 ](https://turbo.net.cn/docs/getting-started/add-to-existing-repository#edit-gitignore)`.gitignore`

将 `.turbo` 添加到您的 `.gitignore` 文件中。 `turbo` CLI 使用这些文件夹来持久化日志、输出和其他功能。

```text
.turbo
```

### [向根目录 ](https://turbo.net.cn/docs/getting-started/add-to-existing-repository#add-a-packagemanager-field-to-root-packagejson)`package.json` 添加 `packageManager` 字段

Turborepo 利用您的包管理器信息优化您的存储库。要声明您正在使用的包管理器，如果您的根目录 `package.json` 中尚未包含 `packageManager` 字段，请添加它。

```json
{
+  "packageManager": "pnpm@10.0.0" #如果想要直接在根目录上运行所有项目的安装或者其他指令,建议添加
}
```

[设置包管理器工作区](https://turbo.net.cn/docs/getting-started/add-to-existing-repository#set-up-package-manager-workspaces)

对于[多包工作区](https://vercel.com/docs/glossary#multi-package-workspace)，您需要配置包管理器以识别您的工作区结构。

`workspaces` 字段告诉您的包管理器哪些目录包含您的包。常见模式包括 `apps/*` 用于应用程序，`packages/*` 用于共享库。

```yaml
packages:
  - "apps/*"
  - "packages/*" #如果不添加你的项目在这里，那么在统一启动的时候不会启动你新加的项目，或者新添加一个分类文件夹时，比如server/*
```

[使用 ](https://turbo.net.cn/docs/getting-started/add-to-existing-repository#run-tasks-with-turbo)`turbo` 运行任务

```shell
turbo build check-types
```

这将同时运行 `build` 和 `check-types` 任务。您的[工作区](https://turbo.net.cn/docs/crafting-your-repository/structuring-a-repository#anatomy-of-a-workspace)的依赖图将用于以正确的顺序运行任务。

**不更改代码，再次尝试运行 **`build` 和 `check-types`

```shell
turbo check-types build
```

应该会看到终端有如下输出

```shell
Tasks:    2 successful, 2 total
Cached:    2 cached, 2 total
Time:    185ms >>> FULL TURBO
```

恭喜！**您刚刚在几毫秒内完成了代码构建和类型检查**。

要了解 `turbo` 如何实现这一点，请查阅[缓存文档](https://turbo.net.cn/docs/crafting-your-repository/caching)。

[通过运行 ](https://turbo.net.cn/docs/getting-started/add-to-existing-repository#begin-developing-by-running-dev-with-turbo)`turbo` 启动 `dev` 进行开发

```shell
turbo dev
```

# 对单仓库的某个项目进行单独的依赖安装/删除操作

### 1. 进入特定项目目录安装依赖

```bash
# 为 my-app 项目添加依赖
cd my-app
pnpm add react-query

# 为 vite-project 项目添加依赖  
cd vite-project
pnpm add axios

# 添加开发依赖
cd my-app
pnpm add -D typescript
```

### 2. 使用包管理器的工作区命令

```bash
# 为特定包添加依赖
pnpm add react-query --filter my-app
pnpm add axios --filter vite-project

# 添加开发依赖
pnpm add -D typescript --filter my-app
```

### 3. 删除依赖

```bash
# 在项目目录中删除
cd my-app
pnpm remove react-query

# 或从根目录删除
pnpm remove react-query --filter my-app
```


