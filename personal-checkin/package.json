{
  "name": "personal-checkin",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"cd server && npm run dev\" \"cd frontend && npm run dev\" \"electron .\"",
    "build:all": "cd frontend && npm run build && cd ../server && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "@electron-forge/maker-squirrel": "^7.11.1",
    "concurrently": "^9.2.1",
    "electron": "^40.6.0",
    "electron-forge": "^5.2.4"
  }
}
